styles need to be camel case

dishes.map((dish, i) => ({ id: i, title: dish }));

can use url in place of image

<React.Fragment /> allows for components to sit next to eachother (wraper)
    - shorthand <></>

- className instead of class

- array desctructuring: const [, , item] = ["one", "two", "three"]; console.log(item);

- useState to manage local variables

- npm test instead of npm start

- expect(function).toBe(answer)

- testing-library

- react router
- cmd: `npm install react-router@next react-router-dom@next`

- pages.js
- `npm install history`



